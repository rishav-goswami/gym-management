{
  "conditions": [],
  "parameters": {
    "chat_enabled": {
      "defaultValue": { "value": "true" },
      "valueType": "BOOLEAN",
      "description": "Emergency platform-level switch for direct chat."
    },
    "gym_support_enabled": {
      "defaultValue": { "value": "true" },
      "valueType": "BOOLEAN",
      "description": "Emergency switch for trainer coaching and gym operations support."
    },
    "platform_support_enabled": {
      "defaultValue": { "value": "true" },
      "valueType": "BOOLEAN",
      "description": "Emergency switch for consumer account and product support cases."
    },
    "support_images_enabled": {
      "defaultValue": { "value": "true" },
      "valueType": "BOOLEAN",
      "description": "Controls private image attachments in support cases."
    },
    "attendance_qr_enabled": {
      "defaultValue": { "value": "true" },
      "valueType": "BOOLEAN",
      "description": "Emergency platform-level switch for rotating QR attendance."
    },
    "progress_photos_enabled": {
      "defaultValue": { "value": "true" },
      "valueType": "BOOLEAN",
      "description": "Emergency platform-level switch for progress-photo uploads."
    },
    "personal_spaces_v1": {
      "defaultValue": { "value": "false" },
      "valueType": "BOOLEAN",
      "description": "Release gate for the consumer-first personal fitness experience. Enable only after migration and acceptance checks."
    },
    "maintenance_message": {
      "defaultValue": { "value": "" },
      "valueType": "STRING",
      "description": "Optional global maintenance message shown by clients."
    }
  },
  "parameterGroups": {}
}
