# Gym Management Platform Console

Private Flutter Web control plane for Gym Management platform operators. This
application is built and deployed independently from `../gym_app` and must
never be added to mobile store builds.

See [`../../docs/PLATFORM_CONSOLE.md`](../../docs/PLATFORM_CONSOLE.md) for
access provisioning, local development, security boundaries, and deployment.
